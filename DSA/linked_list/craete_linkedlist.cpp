/* create linked list by user only  */
#include<iostream>
#include <bits/stdc++.h>
using namespace std;

class node{
    public:
        int data;
        node*next;
};

int main(){

    int n;
    node*first=NULL;
    first =new node();

    cout<<"Enter size of linked list:";
    cin>>n;
    while(n){

        node*temp=NULL;
        temp=new node();
        n--;
    }
}